[package]
name = "coverlab-rust-client"
version = "0.1.0"
edition = "2021"

[dependencies]
base64 = "0.22"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "rustls-tls"] }
