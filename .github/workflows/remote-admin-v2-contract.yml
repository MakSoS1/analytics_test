name: Remote Admin Anomaly V2 Contract Tests

on:
  push:
    branches: [remote-admin-anomaly-lab-v2]
    paths:
      - 'remote-admin-anomaly-lab/configs/**'
      - 'remote-admin-anomaly-lab/src/**'
      - 'remote-admin-anomaly-lab/scripts/**'
      - 'remote-admin-anomaly-lab/tests/**'
      - 'remote-admin-anomaly-lab/windows/**'
      - 'remote-admin-anomaly-lab/requirements.txt'
      - '.github/workflows/remote-admin-v2-*.yml'
      - '.github/legacy/**'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: remote-admin-v2-contract-${{ github.ref }}
  cancel-in-progress: true

jobs:
  contract-tests:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: remote-admin-anomaly-lab/requirements.txt
      - name: Install full test runtime
        working-directory: remote-admin-anomaly-lab
        run: python -m pip install -r requirements.txt
      - name: Run all V1 regression and V2 contracts
        working-directory: remote-admin-anomaly-lab
        env:
          PYTHONPATH: src
        run: pytest -q tests
