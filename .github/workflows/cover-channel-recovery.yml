name: Cover Channel Recovery Resume (Deprecated V2)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-24.04
    steps:
      - name: V2 recovery is retired
        run: |
          echo 'Contract-v2 recovery is intentionally retired.'
          echo 'Use "Cover Channel V3 Recovery" for contract-v3 smoke-gated, resume-first collection and model training.'
          echo 'No traffic generation or model training is performed by this legacy workflow.'
