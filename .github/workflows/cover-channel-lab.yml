name: Cover Channel Dataset Lab (Deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-24.04
    steps:
      - name: Use recovery workflow instead
        run: |
          echo 'This legacy workflow is intentionally retired.'
          echo 'Use "Cover Channel Recovery Resume" for smoke-gated, contract-v2, resume-first corpus generation.'
          echo 'No traffic generation is performed by this workflow.'
