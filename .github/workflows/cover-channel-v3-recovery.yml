name: Cover Channel V3 Recovery (Deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-24.04
    steps:
      - name: Use audit v4 recovery instead
        run: |
          echo 'Contract-v3 recovery is intentionally retired from automatic execution.'
          echo 'Use Cover Channel Audit V4 Recovery for smoke-gated resume/full corpus generation.'
          echo 'No traffic generation or model training is performed by this deprecated workflow.'
